Hi
this is my new project pipelin - syntax
********************************************
pipeline{
   agent any
   	stages{
	  stage('build'){
	    steps{
	     sh 'echo "building the application"'
	     //Add commands to build the application
	    }
	   }
	  stage('Test'){
	   parallel{
	    stage('unit Test'){
	      steps{
	       sh 'sleep 5s'
	       sh 'echo "Running unit tests"'
	       }
	      }
	     stage('integration Test'){
	      steps{
	       sh 'echo "Running Integration tests"'
	       //add commands to integration tests
	       }
	      }
	     }
	    }
	  stage('deploy'){
	    steps{
	      sh 'echo "deploy the application"'
	      //Add commands to deploy the application
	     }
	    }
	   }
	  }
